<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Fragment.BottomNav.HomeFragment">

    <include
        android:id="@+id/xToolbar"
        layout="@layout/layout_toolbar" />

    <TextView
        android:id="@+id/xCurDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/xToolbar"
        android:background="@color/grey"
        android:fontFamily="@font/montserrat"
        android:padding="20dp"
        android:text="Today"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:textStyle="bold" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/xCurDate"
        android:background="@color/white">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/xHomeTabLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="20dp"
            android:orientation="horizontal"
            app:tabGravity="start"
            app:tabIndicatorColor="@color/blue"
            app:tabIndicatorGravity="stretch"
            app:tabSelectedTextColor="@color/white"
            app:tabTextColor="@color/subText" />

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/xHomeViewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/xHomeTabLayout" />

    </RelativeLayout>

</RelativeLayout>